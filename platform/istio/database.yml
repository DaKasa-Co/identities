apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: postgresdb
  namespace: postgres
spec:
  hosts:
  - postgresdb
  http:
    - route:
      - destination:
          host: postgresdb
          port:
            number: 5432